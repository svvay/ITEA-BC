# 2. Create lambda functions that calculate `pow`, `sum`, and `multiply`.

sum = lambda x: x + x
multiplay = lambda x: x * x
powe = lambda x, y: x ** y

print(sum(2))
print(powe(3,2))
print(multiplay(3))
